module PipeWire

export CoreConnection,
    CoreInfo,
    CoreMemory,
    Context,
    Client,
    ClientInfo,
    Device,
    DeviceInfo,
    Direction,
    Factory,
    FactoryInfo,
    Filter,
    FilterBuffer,
    FilterData,
    FilterIO,
    FilterPort,
    FilterPosition,
    BufferHeader,
    BufferBitmap,
    BufferBusy,
    BufferCursor,
    BufferRegion,
    BufferSyncTimeline,
    Global,
    Link,
    LinkInfo,
    LinkState,
    LoopChannel,
    EventSource,
    IdleSource,
    IOSource,
    SignalSource,
    TimerSource,
    MainLoop,
    MappedStreamData,
    Metadata,
    ModuleInfo,
    Node,
    NodeInfo,
    NodeState,
    ParamInfo,
    Permission,
    PipeWireError,
    PipeWireModule,
    Pod,
    Properties,
    Proxy,
    Port,
    PortInfo,
    Registry,
    Stream,
    StreamBuffer,
    StreamBufferInfo,
    StreamControl,
    StreamData,
    StreamIO,
    StreamMetadata,
    StreamTime,
    ThreadLoop,
    STREAM_AUTOCONNECT,
    STREAM_DRIVER,
    STREAM_DONT_RECONNECT,
    STREAM_ALLOC_BUFFERS,
    STREAM_ASYNC,
    STREAM_EARLY_PROCESS,
    STREAM_EXCLUSIVE,
    STREAM_INACTIVE,
    STREAM_MAP_BUFFERS,
    STREAM_NO_CONVERT,
    STREAM_TRIGGER,
    FILTER_ASYNC,
    FILTER_CUSTOM_LATENCY,
    FILTER_DRIVER,
    FILTER_INACTIVE,
    FILTER_PORT_ALLOC_BUFFERS,
    FILTER_PORT_MAP_BUFFERS,
    FILTER_TRIGGER,
    LOOP_IO_ERR,
    LOOP_IO_HUP,
    LOOP_IO_IN,
    LOOP_IO_OUT,
    Audio,
    SPA,
    Video,
    add_port!,
    allocate_buffer!,
    audio_format,
    buffer_data,
    buffer_bitmap,
    buffer_busy,
    buffer_control,
    buffer_cursor,
    buffer_header,
    buffer_info,
    buffer_metadata,
    bytes,
    capacity,
    connect!,
    core_properties,
    create_object,
    data_pointer,
    data_fd,
    data_flags,
    data_map_offset,
    data_type,
    dequeue_buffer,
    dequeue_buffer!,
    disconnect!,
    dsp_buffer,
    destroy_global!,
    destroy_object!,
    enum_params!,
    flush!,
    filter_name,
    filter_nsec,
    filter_properties,
    filter_state,
    emit_event!,
    globals,
    get_permissions!,
    in_thread,
    is_driving,
    is_lazy,
    is_mapped,
    isrunning,
    enable!,
    bound_id,
    interface_type,
    library_version,
    map_data,
    main_loop,
    node_id,
    metadata_bytes,
    metadata_count,
    metadata_pointer,
    metadata_size,
    metadata_type,
    pod_type,
    pod_value,
    pong!,
    position_snapshot,
    proxy_id,
    queue_buffer!,
    remove_port!,
    return_buffer!,
    quit!,
    roundtrip,
    report_error!,
    run!,
    set_active!,
    set_error!,
    set_chunk!,
    set_buffer_size!,
    set_param!,
    set_property!,
    set_control!,
    set_rate!,
    signal!,
    start!,
    stop!,
    send_command!,
    stream_state,
    stream_control,
    stream_name,
    stream_nsec,
    stream_properties,
    stream_time,
    subscribe_params!,
    sync!,
    sync_timeline,
    trigger_process!,
    update_permissions!,
    update_io!,
    update_timer!,
    update_params!,
    update_properties!,
    video_format,
    video_crop,
    video_damage,
    video_transform,
    hello!,
    clear!,
    writable_bytes,
    with_main_loop,
    with_thread_loop_lock,
    with_registry

include("generated/LibPipeWire.jl")
include("main_loop.jl")
include("properties.jl")
include("thread_loop.jl")
include("loop_source.jl")
include("loop_channel.jl")
include("core.jl")
include("proxy.jl")
include("spa_types.jl")
include("spa.jl")
include("objects.jl")
include("stream.jl")
include("filter.jl")

"""
    library_version() -> VersionNumber

Return the version of the loaded PipeWire client library.
"""
library_version() = VersionNumber(unsafe_string(LibPipeWire.pw_get_library_version()))

end # module PipeWire
